from pathlib import Path

ROOT = Path("../../../Datasets/ieee_grss_2022")
IMG_PATHS = Path(ROOT / "gt")
MASK_PATHS = Path(ROOT / "mask")
PNG_IMG_PATHS = Path(ROOT / "png_gt")
PNG_MASKS_PATHS = Path(ROOT / "png_mask")
